### Setup de ambiente - Poetry
- `poetry install` – Instala as dependências.
- `poetry shell` – Entra no ambiente virtual Poetry.
- `python --version` – Confirma versão do Python.
- Dependências base: `azure-functions`.
